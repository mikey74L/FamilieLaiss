@using FamilieLaissResources.Resources.Dialogs.Choose.Video

@if (ItemsCount == 0 && !HasError)
{
    <MudText Typo="Typo.h5">@ChooseVideoDialogMessageOutputRes.MessageNoItems</MudText>
}

@if (HasError)
{
    <MudText Color="Color.Error" Typo="Typo.h5">@ChooseVideoDialogMessageOutputRes.MessageErrorLoading</MudText>
}

@code
{
    [Parameter]
    public int ItemsCount { get; set; }

    [Parameter]
    public bool HasError { get; set; }
}
